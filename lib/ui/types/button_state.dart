enum XButtonState {
  /// State of the button when it is enabled and not pressed.
  enabled,

  /// State [XButtonState.disabled], used for widget with disabled state
  disabled,

  /// State [XButtonState.loading], used for widget with loading indicator
  loading,
}